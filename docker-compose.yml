version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
      - "5140:5140/udp"
    volumes:
      - ./database:/app/../database
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend